<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TripExpensePlanner" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/primaryDark</item>

        <!-- Applies to every plain <Button> in the app (Material auto-inflates them as
             MaterialButton under this theme), so every button gets the same rounded
             corners and sentence-case text without editing each layout individually. -->
        <item name="materialButtonStyle">@style/AppButton</item>
    </style>

    <style name="AppButton" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">@dimen/button_corner_radius</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">@dimen/text_size_subtitle</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <!-- Used for the small in-card text actions (Edit / Delete / Cancel Reminder). -->
    <style name="AppButton.Borderless" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
        <item name="android:textSize">@dimen/text_size_body</item>
    </style>
</resources>
